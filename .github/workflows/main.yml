name: CI

on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@v1
    - name: Run Scoop
      run: ./install.ps1
      shell: powershell
    - name: Run the installer
      run: ./install.sh
      shell: bash
